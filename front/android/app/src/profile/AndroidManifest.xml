<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The INTERNET permission is required for profile builds so DevTools and
         Observatory can connect to the running application.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>
</manifest>
