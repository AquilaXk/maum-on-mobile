{
  "name": "maum-on-mobile-front",
  "version": "0.1.0",
  "private": true,
  "main": "src/main.tsx",
  "scripts": {
    "start": "expo start",
    "android": "expo start --android",
    "ios": "expo start --ios",
    "lint": "node scripts/validate-project.mjs --lint",
    "test": "node --test scripts/*.test.mjs",
    "typecheck": "tsc --noEmit",
    "build": "npm run typecheck && node scripts/validate-project.mjs --build"
  },
  "dependencies": {
    "expo": "~56.0.4",
    "expo-status-bar": "~56.0.4",
    "react": "19.2.3",
    "react-native": "0.85.3"
  },
  "devDependencies": {
    "@types/react": "~19.2.2",
    "typescript": "~6.0.3"
  }
}
